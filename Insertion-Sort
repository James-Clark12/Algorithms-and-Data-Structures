/* Insertion sort */

// const insertionSort = (array) => {
//   for (let i = 0; i< array.length; i++) {
//     let j = i;
//     while(j > 0 && array[j] < array[j-1]) {
//       swap(array, j, j-1);
//       j = j - 1;
//     }
//   }
//   return array;
// }
